server:
  port: 8081 # ?? 8080?????

spring:
  application:
    name: personnel-service
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/personnel_db?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: ${MYSQL_SECRET} # <--- ???????????

# MyBatis Plus ??
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl # ?????? SQL?????
  global-config:
    db-config:
      logic-delete-field: deleted # ??????????